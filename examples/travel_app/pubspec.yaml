# Copyright 2025 The Flutter Authors.
# Use of this source code is governed by a BSD-style license that can be
# found in the LICENSE file.

name: travel_app
description: "An app that generates travel user interfaces on the fly using AI."
publish_to: "none" # Remove this line when we're ready to publish to pub.dev
version: 0.1.0

environment:
  sdk: ">=3.9.2 <4.0.0"
  flutter: ">=3.35.7 <4.0.0"

resolution: workspace

dependencies:
  flutter:
    sdk: flutter
  genui: ^0.6.0
  genui_google_generative_ai: ^0.6.0
  gpt_markdown: ^1.1.4
  intl: ^0.20.2
  json_schema_builder: ^0.1.3
  logging: ^1.3.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  integration_test:
    sdk: flutter
  network_image_mock: ^2.1.1

flutter:
  uses-material-design: true
  assets:
    - assets/travel_images/
    - assets/booking_service/
